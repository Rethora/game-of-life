export const GenerationDisplay = ({ generation }) => (
    <div className="display">
        <div className="label">Generation:</div>
        <div className="generation">{generation}</div>
    </div>
)